define(function(){
    // must return "delete" or "update"
    return function(event){
        // console.log(event);
        if(event){
            return "";
        }else{
            return "";
        }
    }
})
