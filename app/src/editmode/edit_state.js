define(function(){
    //edit selected state's properties
    return function(d,inputValue,context){
        console.log(d,inputValue,context);
        //restart force layout w/ new data
        //context.force.start();

    }
});
